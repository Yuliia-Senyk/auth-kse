doctype html
html
    head
        title Dashboard
    body
        h1 Welcome to the Dashboard
        p You are logged in as #{user}.
        form(action='/logout', method='POST')
            input(type='submit', value='Logout')
        p
            a(href='/protected') Access protected resource
